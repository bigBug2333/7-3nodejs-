/* 

  npm install 包名;
  npm install --save-dev 包名： 项目要用到，仅仅在开发的饿时候用到。

  npm install -g 包名;全局安装不是说这个项目要用，而是我们用命令
  

  npm install;  自动package.json找到所有的依赖，全局安装
  npm install --production: 找到package.json中的 dependencies安装

*/